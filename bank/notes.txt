learned:
1. DNS lookup
2. apache default page `vuln`
3. if we add `GIF8` at the top and run `file <file>` cmd it will say `GIF image`
4. in mysql db you can type `\! /bin/sh` to have a shell
5. linpeas.sh
6. `find / - -perm -4000 2>/dev/null` to find suid
